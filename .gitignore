# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code local (machine-specific) settings
.claude/settings.local.json

# Local secrets — never commit real API keys
.env
.env.*
!.env.example

# One-off local TTS comparison output (audio files, not part of the app)
audio-samples/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local intermediate artifact from scripts/generate-lesson-audio.ts
scripts/.whisper-cache.json

# Черновые результаты локального прогона scripts/generate-new-lesson.ts —
# для ручной проверки, не часть приложения, пока не "усыновлены"
scripts/output/
.vercel
.env*
